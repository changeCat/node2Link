name = "sub-worker"
main = "_worker.js"
compatibility_date = "2025-07-23"
keep_vars = true

#[[kv_namespaces]]
#binding = "KV"          # KV 绑定名必须为 KV
#id = "your-kv-id"

# 使用 `wrangler secret put ADMIN_PASSWORD` 设置登录密码。
# 建议同时使用 `wrangler secret put SESSION_SECRET` 设置独立会话签名密钥。
# [vars]
# ADMIN_USERNAME = "admin"
