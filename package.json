{
  "name": "node2link-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node scripts/local-server.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "pages:build": "npm run check && npm test && npm run build && npm run smoke",
    "smoke": "node scripts/smoke.mjs",
    "test:e2e": "playwright test",
    "benchmark": "node scripts/benchmark-storage.mjs",
    "benchmark:requests": "node scripts/benchmark-kv-requests.mjs"
  },
  "dependencies": {
    "@keeex/qrcodejs-kx": "1.0.2",
    "lucide": "1.31.0"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "esbuild": "0.28.2"
  }
}
