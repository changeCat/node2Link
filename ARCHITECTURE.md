# 架构与数据兼容性

## 部署契约

仍使用 Cloudflare Pages 的 Git 集成：推送 `main` 后执行 `npm run pages:build`，发布 `dist`。只需要现有 `KV` 绑定和环境变量，不新增 Durable Object、D1、队列、独立后端或部署步骤。`.github/workflows/ci.yml` 继续执行同一套校验。

## 模块职责

```text
src/worker/app.js          路由与统一错误边界
src/worker/routes/         HTTP 参数、认证后的管理操作、响应
src/worker/services/       节点导入、订阅生成、候选节点
src/worker/domain/         格式选择、节点校验、模板、兼容转换
src/worker/storage/        KV 数据读取、追加记录、派生快照
src/worker/adapters/       上游 HTTP、转换服务、Telegram
src/worker/ui/             服务端页面模板与页面数据
src/client/                独立浏览器脚本，构建时打包到静态资源
```

页面通过 `application/json` 数据块传递初始状态，序列化时转义 `<`。浏览器脚本与公共 CSS 一样走 `/assets/*`，使用内容版本号缓存。订阅链接、管理路由和导入 API 路径保持不变。

## 存储规则

- `null` 表示不存在；读取失败或 JSON 损坏抛出 `StorageError`，返回 503，不用空集合继续覆盖原数据。
- 旧 `LINK.txt`、`/LINK.txt`、设置、节点数组、分享详情与索引继续读取，不在普通 GET 中删除或重写旧数据。
- 主订阅 ID 优先使用已有值。新安装的候选 ID 从已配置的秘密派生，并在成功登录后保存到独立的 `NODE2LINK.identity.json`，避免首次并发请求生成不同链接。已经保存的 ID 不随密码变更而改变。
- `NODE2LINK.v2.nodes.*`：每次导入以一个独立记录发布完整批次；删除记录保存节点 ID。不同批次不会争用一个整体数组。读取按记录顺序合并、去重，并应用删除记录。
- `NODE2LINK.cache.nodes.v2`：可重建的节点派生快照，记录明确的已应用记录集合。读取仍检查未应用的追加记录，所以并发或陈旧快照不能覆盖新批次。每积累 16 条未应用记录尝试更新快照；失败不影响数据。只有派生缓存允许在读取异常时丢弃并重建。
- `NODE2LINK.v2.settings.*`：保存发生变化的设置分区。显示、入口、转换、客户端展示各自合并，避免不同分区同时保存互相覆盖。
- `NODE2LINK.v2.shares.*`：分享正文与索引摘要在同一个 KV 写入中发布；重置链接时，旧 ID 删除标记和新分享一并发布。列表优先使用 metadata 摘要，查看详情才读取正文。旧索引遗漏的分享可通过详情键发现。
- `NODE2LINK.v2.main.*`：正文与元数据写入一个版本记录。倒序时间键让当前版本和上一版本仅需读取最近两条，继续提供“恢复上次保存版本”。

这些记录是权威数据，不是可以随意删除的缓存。删除节点或分享会使其从当前视图消失、链接失效，但历史记录仍保留。不要单独删除删除标记，否则旧内容可能重新出现。当前没有自动清理或压缩权威记录；写入次数长期增加时，存储占用和索引扫描成本会增长。

### KV 一致性的边界

此实现避免不同追加批次和不同设置分区的整体覆盖，不把 KV 伪装为事务数据库。跨地区读取、列表和删除标记仍可能短暂延迟；同一分享或同一设置分区同时修改时，按记录键确定最终版本。独立实例的时钟不构成全局提交顺序。

节点数量和字节数上限按当前可见数据校验，无法提供跨地区并发事务配额。并发重复导入可能分别报告新增，最终读取会去重。一次删除会覆盖当次可见的相同内容副本；与尚不可见的新导入竞争时，需要在同步后重新检查。

若以后要求立即撤销、严格并发配额或高频写入，应升级为有事务的存储；本次为保持现有部署方式没有引入额外绑定。

## 订阅流程

1. 显式格式参数优先于 User-Agent；转换服务的回源请求返回 Base64。
2. Base64 请求在本地有界并发读取上游，再附加 API 节点并应用客户端兼容规则。
3. 其他格式将“本站直接节点回源地址 + 原始上游地址”提交给所选转换器。内部 `?base64&source=direct` 回源仍经过原订阅链接校验，只输出本站直接节点，避免上游被重复拉取。
4. 单次远程请求超时覆盖响应头和响应体，默认 8 秒。多个默认转换器共享时间预算，并为后备服务预留机会；自建模式不回退到默认服务。
5. 上游默认最多 6 个并发，单响应最多 8 MB，单次聚合最多 32 MB。异常来源继续跳过并通过响应头报告；指定转换格式失败返回 502，不返回伪装成成功结果的其他格式。

订阅输出继续禁用缓存。响应提供实际格式、订阅与应用处理耗时，以及可见的上游失败数。请求统计在结果产生后记录状态码和耗时；现有最近 30 天、最多 500 条访问统计保留。远程诊断只记录域名、状态、错误类型和耗时，不记录完整 URL、Token 或节点内容。

## 校验与回滚

`npm run pages:build` 扫描所有 JS/MJS 语法，校验 Worker 模块图，运行回归测试并构建，再通过 `npm run smoke` 检查实际构建产物的管理页面、静态脚本引用、主/API 订阅及分享生命周期。测试覆盖旧数据读取、并发批次、读取/写入故障、分享重置、主订阅备份、派生快照、响应体超时、转换回退、格式与脱敏。

本地预览与测试共用支持 metadata、分页、排序和 TTL 的 MemoryKV。它不模拟 Cloudflare 的跨地区传播；本地数据只存在内存中。使用 `NODE2LINK_DEV_API_SUBSCRIPTION_ENABLED=true` 可在本地开启 API 订阅。

升级前的旧版本不能识别 `NODE2LINK.v2.*`。直接回滚到旧代码只会看到升级前的旧数据，不会包含新版期间的修改；新记录没有丢失，重新部署本版或兼容本存储格式的版本即可恢复读取。需要回滚旧代码时，应先备份完整 KV 并规划数据导出，不能通过删除新记录实现回滚。
