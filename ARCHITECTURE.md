# 架构与存储

## 部署契约

Cloudflare Pages Git 集成保持不变：推送 `main` 后执行 `npm run pages:build`，发布 `dist`。使用原有 `KV` 绑定和环境变量，不新增 D1、Durable Object、队列或后端。GitHub CI 独立执行构建及桌面/手机浏览器测试，Pages 构建无需安装浏览器。

本版是允许手动重新录入数据的存储格式升级。主订阅、设置、分享及 API 节点不再回读旧固定键或 `NODE2LINK.v2.*`，不执行自动迁移，也不删除原记录。旧数据不会自动显示；部署后从本地备份重新添加内容、设置及分享。固定身份 `NODE2LINK.identity.json`、API 模板/已保存 Token 及请求日志继续直接使用现有键，因为没有兼容扫描成本。只保存在旧 bootstrap 历史中的 API Token 需要重新初始化。

## 模块职责

| 路径 | 职责 |
|---|---|
| `src/worker/app.js` | 路由、提前鉴权及统一错误边界 |
| `routes/` | HTTP 参数、登录和管理操作 |
| `services/` | 订阅生成、导入、候选节点及仪表盘数据 |
| `domain/` | 格式、节点、模板和客户端兼容规则 |
| `storage/` | 权威记录及管理视图 |
| `adapters/` | 上游 HTTP、转换器、Telegram |
| `ui/`、`src/client/` | 服务端页面及独立浏览器脚本 |

页面数据通过转义 `<` 的 JSON 数据块传递。脚本与 CSS 使用带内容版本号的 `/assets/*` 静态资源。路由、订阅格式、功能开关、节点上限和环境变量含义保持不变。

## 存储规则

- 不存在的键与故障严格区分。读取失败、JSON 损坏或被引用版本不可见均抛出 `StorageError`，不回退到旧格式、旧权限或空数据后覆盖。
- `NODE2LINK.v3.settings.<section>`：显示、入口、转换、客户端和日志五个独立当前记录。读取只执行 get，不扫描历史；不同分区并发保存互不覆盖。页面每次保存一个分区。批量 `all` 调用写入各分区，不提供跨键事务。
- `NODE2LINK.identity.json`：主订阅 ID。成功登录后才初始化，候选值由配置的秘密确定；已保存的 ID 不随密码变化。
- `NODE2LINK.v3.main.head`：当前及上一个版本的键。正文与元数据在独立的 `NODE2LINK.v3.main.version.*` 不可变记录中。先写正文，再一次写入 head 发布当前/备份指针；发布失败不改变可见正文与上次备份。读取无需 List，仍支持主动清空、版本冲突提示和恢复上次保存。每份正文上限仍为 20 MiB，避免将两份大正文放入一个超限 KV 值。
- `NODE2LINK.v3.shares.<id>`：当前分享正文；同一次 put 的 metadata 保存管理摘要。编辑覆盖同一个 ID 的当前记录，不累计编辑历史。
- `NODE2LINK.v3.revoked.<id>`：永久撤销标记。普通修改不覆盖该标记，即使旧编辑在删除后才完成写入，链接也不能恢复。公开请求每次按 ID 直接读取详情及撤销标记，不依赖管理摘要缓存。
- 分享重置先写带 `activationId` 的待启用记录，再通过旧 ID 的撤销标记发布 `replacementId`。后一个写入同时决定旧 ID 撤销、新 ID 启用。后一步失败时，旧链接保持可用，新记录在公开请求及管理列表中均隐藏；可重新发起重置。重置后的详情额外按键读取激活标记。KV 跨键传播仍受其原生一致性限制，无法承诺各地区同时切换。
- `NODE2LINK.v3.nodes.*`：保留 API 导入的不可变批次和删除记录，避免不同并发导入覆盖一个整体数组。删除覆盖当前可见的同内容副本。节点读取不再读取旧节点数组。
- `NODE2LINK.cache.nodes.v3`：节点派生快照，保留明确的已应用记录集合；读取仍扫描新日志并重放未应用记录，旧快照不会吞掉新导入。每累计 16 条未应用记录尝试更新，失败不影响权威数据。
- `NODE2LINK.api-subscription.settings.json`：API 模板及显式保存/轮换的 Token。`NODE2LINK.api-subscription.bootstrap.v3` 保存首次初始化候选 Token，不再扫描 bootstrap 历史。初始化只允许登录且同源的 POST，同实例并发初始化合并；独立的 bootstrap 键不会覆盖同时发生的模板保存或 Token 轮换。
- `NODE2LINK.request.*`：请求日志保留 30 天，统计最近最多 500 条，沿用 metadata 摘要。

主订阅历史正文、失败发布的孤立正文和撤销标记不会自动清理；它们不参与正常主/分享详情的列表扫描。API 节点日志和管理端撤销列表仍可能随使用增长。不能删除撤销标记来“清缓存”，否则可能重新启用旧内容。

## KV 用量与一致性

无会话且无 Token 的首页、无效多级路径、非法编码、空/超长 Token、不支持的方法及已知修改路由的跨源请求在读取 KV 前结束。有效形状但不匹配的单段 Token 仅读取当前入口键，随后结束；正常请求复用该读取。Token 可以与页面名称相同，也支持编码后的斜杠。

分享、节点、候选及退出管理 API 先校验会话，再执行各自操作，不读取无关站点设置。公开导入先校验 API Token；仅在需要 Telegram 成功通知时异步读取全站设置。

完整设置、身份、Token、主正文、分享正文与撤销标记均直接读 KV。仅 `/s/<id>` 的非凭据设置使用 60 秒实例内视图；分享摘要、节点管理索引、请求统计使用 15 秒视图。写入前后失效当前实例缓存，防止在途旧加载重新填入缓存。故障不缓存成空结果，过期后不以旧权限放行。

KV 本身最终一致；不同地区可能在 60 秒或更久后才看到修改，不能承诺即时撤销。主编辑器版本检查不是原子 CAS，同一分区/分享同时修改按最终 KV 写入结果收敛；节点总量与去重校验也不是跨地区事务。固定键受每秒一次写入限制，明确的 429 最多重试两次；普通故障不重试，持续配额耗尽仍报错。详见 [KV 一致性](https://developers.cloudflare.com/kv/concepts/how-kv-works/) 和 [限制](https://developers.cloudflare.com/kv/platform/limits/)。

### 本地操作数

`npm run benchmark:requests` 使用 MemoryKV 测量完整请求，显式开启全量日志便于比较：

| 请求 | Get（冷/热） | List（冷/热） |
|---|---:|---:|
| 无 Token 的匿名首页、无效多级路径 | 0 / 0 | 0 / 0 |
| 登录页 | 6 / 6 | 0 / 0 |
| Token 主订阅，API 节点关闭 | 8 / 8 | 0 / 0 |
| 主/独立分享 `/s/<id>`，API 节点关闭 | 7 / 3 | 0 / 0 |
| 主订阅附加 API 节点，空节点日志 | 8 / 4 | 1 / 1 |

重置后的分享因检查激活标记增加一次 get。节点历史分页、未缓存正文等会增加实际调用。每次正常订阅在完整日志模式写一条日志，采样后相应减少。读更多固定键换取零 List，利用 KV 读额度远高于 List 额度的特点；并非所有类型操作都下降。

`npm run benchmark` 模拟 100、1000、10000 次修改。100 个分享无论编辑多少次，冷管理摘要为 2 次 List、0 次 Get，15 秒内热读为 0；设置固定 6 次 Get、0 次 List。API 节点历史仍需扫描；无派生快照的大规模冷重建可能受 Worker 子请求上限约束，此版本未解决无限历史规模。以上是本地调用数，不代表 Cloudflare 延迟或账户总量。

### 日志模式

未显式配置时默认采样 10%；`REQUESTLOG=1` 保持完整记录，`REQUESTLOG=0` 保持关闭。管理页保存的模式/比例优先。采样模式随机保留正常响应，失败响应全部记录；关闭模式不写日志。日志元数据包含采样信息，页面显示实际留存条数，不能直接推算总访问量或失败率。历史日志不会因切换模式删除，不承诺基于单实例内存的全账户每日硬上限。

### 后续适用 D1 的场景

D1 适合节点并发更新、撤销事务、索引查询及日志聚合，可进一步移除 API 节点日志扫描；但需新增数据库绑定、表结构和初始化流程。D1 单行/字符串上限为 2 MB，当前 20 MiB 主正文需要分块设计，不能直接用一行替换。当前为了保留绑定和环境变量选择 KV。参见 [D1 事务批次](https://developers.cloudflare.com/d1/worker-api/d1-database/#batch)、[计费](https://developers.cloudflare.com/d1/platform/pricing/)、[限制](https://developers.cloudflare.com/d1/platform/limits/)。

## 会话、导入与订阅流程

会话签名绑定账号、密码及 SESSION_SECRET，修改后旧会话失效。请求正文按流字节计数，主订阅、登录、分享和设置共用有界读取；导入最多 12 MiB，15 秒超时，伪造 Content-Length 不能绕过限制。公开导入凭据按请求头、URL、正文优先级校验，保留 JSON/表单 Token 接口。

订阅格式选择、第三方兼容和上游流程保持不变：
1. 显式格式参数优先，转换器回源返回 Base64。
2. Base64 有界并发读取上游，再附加 API 节点并应用客户端兼容规则。
3. 其他格式提交“直接节点回源地址 + 原始上游地址”给转换器；内部回源仍需校验订阅权限，避免重复拉上游。
4. 远程超时覆盖响应头和响应体，默认 8 秒；多个默认转换器共享预算并预留后备机会，自建模式不回退默认服务。
5. 上游最多 6 个并发，单响应 8 MB、聚合 32 MB；部分失败通过响应头报告，转换失败返回 502。
6. 暂停/到期在请求上游之前检查；订阅响应继续 no-store。

仪表盘仍为按需聚合，无轮询或额外统计表；API 关闭时不读取 API 节点。选择器按需先加载本地节点，再补充上游，支持失败重试、每批 100 条显示和全筛选结果选择。编辑器草稿/统计防抖 250 ms，保存和离开前补齐；保留原生时间选择和客户端状态。

`Server-Timing` 提供固定阶段名称与时长，不包含节点、域名或 Token。远程诊断也不记录完整 URL、Token 或正文。

## 校验与回滚

`npm run pages:build` 执行语法/模块图检查、单元回归、构建及产物冒烟；`npm run test:e2e` 在桌面和手机视口检查登录、主编辑器、草稿、版本恢复、分享全生命周期、设置与 API Token 轮换。MemoryKV 支持 metadata、分页和 TTL，但不模拟跨地区传播。

升级前应备份旧数据；本次不提供自动迁移。部署后重新录入主订阅、设置、API 节点，重新创建分享并更新客户端链接。旧键保留但不读取，不能把“旧数据不显示”误认为平台删除了 KV。保留原环境变量 TOKEN 时，该入口仍可用，但输出取决于新格式中重新保存的内容。

回滚到 v2 代码会再次看到 v2/更早记录，看不到 v3 修改；也可能重新启用只在 v3 撤销的旧链接。回滚前应检查两套数据和订阅权限。重新部署 v3 后才能继续读取 v3 数据。不要通过删除身份、撤销标记或版本正文执行回滚。
