# 架构与存储

## 部署契约

Cloudflare Pages 从 `main` 自动构建并发布。运行时必须绑定：

- `DB`：D1 数据库
- `KV`：KV 命名空间

代码面向全新的空数据库和空 KV，不包含历史 KV 数据迁移、读取或无 D1 回退。所有现有环境变量继续作为默认配置或功能开关使用。

## 存储布局

结构化数据统一进入 D1，只有可能超过 D1 单行限制的正文进入 KV。

| 数据 | 存储 | 读取方式 |
|---|---|---|
| 设置、主订阅身份、API 配置 | D1 `node2link_records` | 精确主键或批量主键 |
| 分享、撤销状态 | D1 `node2link_records` | 精确主键；管理页使用有界范围查询 |
| 请求日志 | D1 `node2link_records` | 最多 500 条范围查询 |
| 当前 API 节点 | D1 `node2link_nodes` | 排序键分页 |
| 主订阅 current/previous 指针 | D1 `node2link_records` | 精确主键 |
| 主订阅正文 | KV | D1 指针指定的精确 Get |
| 极端大分享正文 | KV | D1 指针指定的精确 Get |

D1 业务键为：

- `settings.*`
- `identity`
- `main.head`
- `shares.*`
- `revoked.*`
- `api.settings`
- `api.bootstrap`
- `requests.*`

KV 正文键只有 `blob.main.*` 和 `blob.share.*`。

## D1 表

`node2link_records` 保存通用结构化记录：

- `key`：业务主键
- `value`：JSON 或短文本
- `metadata`：列表页所需摘要
- `expires_at`：日志到期时间
- `updated_at`：写入时间

`node2link_nodes` 每行保存一个当前节点，以节点 ID 为主键，以唯一 `sort_key` 保持导入顺序。一次最多导入 100 个节点时拆成 4 条参数安全的 INSERT，并在一个 D1 batch 中提交。删除直接删除当前行，数据量受现有 3,000 节点上限约束。

表结构由部署人员手工维护。首次部署或后续表结构变更时，在 D1 控制台执行对应 migration；运行时代码不执行 DDL，也不自动迁移 schema。

## 写入一致性

主订阅先写新的不可变 KV 正文，再更新 D1 `main.head`。成功后保留当前正文和最近一次正文；更早正文尽力删除。正文写入或 head 更新失败时，原来的当前版本和备份不变。

普通分享的正文和摘要保存在一个 D1 记录中。重置链接时，新分享和旧 ID 的撤销标记通过一个 D1 batch 发布。只有 JSON 后超过 1.8 MB 安全阈值的分享才把正文写入 `blob.share.*`，D1 保存引用。

设置的多分区保存使用 D1 batch。请求日志默认采样 10%，失败请求完整记录；日志保留 30 天，打开统计页时清除已到期行。

## 请求开销

路由先判断方法、路径、功能开关和登录状态。无关扫描请求、非法请求及未授权管理请求在存储访问前结束。

正常公开请求的存储边界：

- 主订阅：D1 精确读取设置和 head，KV 精确读取一次当前正文。
- 普通分享：只访问 D1，KV 操作为零。
- 极端大分享：D1 精确读取分享记录，KV 精确读取一次正文。
- 请求日志：按采样策略写入 D1，不写 KV。
- 管理摘要：使用短时实例内缓存和 D1 有界查询。

在线路径不会调用 KV List。KV 免费套餐的 List 和高频 Write 消耗因此被移除。

## 失败边界

缺少 `DB` 或 `KV` 时立即返回 503 配置错误。D1 读取失败、JSON 损坏或引用正文缺失会抛出 `StorageError`，不会把故障当成空数据。

协议格式、客户端适配、请求体格式和全部环境变量仍保持现有功能；删除的仅是历史存储格式与缺少 D1 时的运行路径。

## 校验

`npm run pages:build` 依次执行源码检查、单元测试、构建和产物冒烟。测试覆盖：

- 已建表 D1 的读写和批量事务
- 100 节点参数限制
- 主订阅 current/previous 发布
- 普通分享零 KV 操作
- 请求日志 D1 TTL
- 未授权请求零存储操作
- 必需绑定错误
- 20 MiB 主订阅和极端分享溢出
