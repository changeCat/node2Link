# 架构与存储

## 部署契约

Cloudflare Pages Git 集成保持不变：推送 `main` 后执行 `npm run pages:build` 并发布 `dist`。原有环境变量含义全部保持不变。生产环境继续绑定 KV，变量名为 `KV`；新增一个 D1 数据库绑定，变量名固定为 `DB`。

账号未绑卡也可以使用 Workers Free 套餐的 D1。免费额度和限制以 Cloudflare 官方文档为准：[D1 计费](https://developers.cloudflare.com/d1/platform/pricing/)、[D1 限制](https://developers.cloudflare.com/d1/platform/limits/)。代码在第一次 D1 操作发现表不存在时自动执行幂等建表，不需要手工运行 SQL；仓库中的 `migrations/0001_hybrid_storage.sql` 可用于审查或显式初始化。

如果暂时没有 `DB` 绑定，Worker 会继续使用当前 KV v3 路径，部署不会因缺少 D1 直接中断。此兼容只用于安全发布，KV 操作优化要在绑定 D1 并重新部署后才会生效。

这次切换不迁移旧结构化数据。用户已备份后，可在 D1 生效后重新录入主订阅、设置、API 节点和分享。原有环境变量 `TOKEN`、`ADMIN_PASSWORD`、`SESSION_SECRET` 等不需修改。

## 模块职责

| 路径 | 职责 |
|---|---|
| `src/worker/app.js` | 路由、提前鉴权、D1/KV 适配及统一错误边界 |
| `routes/` | HTTP 参数、登录和管理操作 |
| `services/` | 订阅生成、导入、候选节点及仪表盘数据 |
| `domain/` | 格式、节点、模板和客户端兼容规则 |
| `storage/` | 权威记录、D1/KV 路由及管理视图 |
| `adapters/` | 上游 HTTP、转换器和 Telegram |
| `ui/`、`src/client/` | 服务端页面及浏览器脚本 |

页面、接口、订阅地址、功能开关、数据上限及现有环境变量保持不变。

## 为什么使用 D1 与 KV 混合存储

D1 适合设置、索引、撤销状态、日志和事务写入。免费套餐每天包含 500 万行读取、10 万行写入，远高于 KV 免费套餐每天 1,000 次写入和 1,000 次 List 的限制。

D1 单个字符串、BLOB 或行最大 2 MB；项目的主订阅允许 20 MiB。分享正文虽限制为 1 MiB，但 JSON 转义后的极端值可能超过 2 MB。把所有正文固定放入 D1 会破坏现有功能。最终布局因此是：

| 数据 | 存储 | 读取方式 |
|---|---|---|
| 设置、身份、API 模板和 Token | D1 | 主键或批量主键读取 |
| 分享摘要、常规正文、溢出指针、撤销状态 | D1 | 主键或有界索引查询 |
| 当前 API 节点 | D1 | 按排序键分页，最多受 3,000 节点上限约束 |
| 请求日志 | D1 | 主键范围查询，最多 500 条 |
| 主订阅 current/previous 指针 | D1 | 主键读取 |
| 主订阅不可变正文 | KV | 按 D1 指针精确 Get |
| 转义后接近 2 MB 的分享正文 | KV | 按 D1 溢出指针精确 Get |

这种布局消除了线上结构化数据的 KV List 和高频 KV 写入。常规分享完全不访问 KV，同时保留全部既有正文上限。

## D1 表与键路由

D1 使用两张表。带主键索引的 `node2link_records` 保存设置、分享、日志和指针：

- `key`：业务主键；
- `value`：JSON 或短文本；
- `metadata`：管理列表所需摘要；
- `expires_at`：请求日志到期时间；
- `updated_at`：写入时间。

`node2link_nodes` 以节点 ID 为主键，以唯一 `sort_key` 保留导入顺序，单行保存一个节点。导入最多 100 个节点时拆为 4 条参数安全的 INSERT 并通过一个 D1 batch 提交；删除直接事务删除当前行。表规模受现有 3,000 节点功能上限约束，不再随重复导入和删除历史增长。

以下数据进入 D1：

- `NODE2LINK.v3.settings.*`
- `NODE2LINK.identity.json`
- `NODE2LINK.v3.main.head`
- `NODE2LINK.v3.shares.*`
- `NODE2LINK.v3.revoked.*`
- `NODE2LINK.v3.nodes.*` 写入时转换为 `node2link_nodes` 当前行
- `NODE2LINK.api-subscription.settings.json`
- `NODE2LINK.api-subscription.bootstrap.v3`
- `NODE2LINK.request.*`

以下键留在 KV：

- `NODE2LINK.v3.main.version.*`
- `NODE2LINK.blob.share.*`

D1 模式不读取也不写入旧 `NODE2LINK.cache.nodes.v3`。节点删除直接作用于 D1 当前表，避免旧派生快照恢复已删除节点。

## 发布、事务与失败边界

主订阅先把新正文写入唯一 KV 键，再更新 D1 head。正文写入或 head 更新失败时，当前版本和备份指针不变。成功发布后保留当前正文及上一个正文，旧一代正文尽力删除；清理失败只留下不可达孤立键，不影响读取。

常规分享把正文和摘要写入同一个 D1 记录。链接重置使用一个 D1 batch 同时写入新记录和旧 ID 撤销标记。只有 JSON 转义后的记录超过 1.8 MB 安全阈值时，才先写唯一 KV blob 并在 D1 保存指针；事务失败时旧链接继续有效，溢出正文只是不可达 blob。删除先写 D1 永久撤销，再尽力删除可能存在的 KV 溢出正文。

设置的批量保存使用一个 D1 batch；分享列表直接读取 D1 metadata，不加载正文。API 节点保存为当前行，删除不留扫描历史；列表每页一次范围查询，最多 3,000 个当前节点，避免每个列表项再发查询。

请求日志默认采样 10%，失败响应始终记录；也可在设置中完整记录或关闭。D1 查询只显示 30 天内记录，打开请求统计时会用 `expires_at` 索引删除已到期行，避免数据库长期积累无效日志。

读取故障、JSON 损坏和被引用正文缺失都会抛出 `StorageError`，不会把故障当作空数据，也不会回退到旧权限记录。

## 旧 KV 数据与性能

绑定 D1 后，旧设置、身份、分享索引、撤销标记、API 节点批次、API 配置和请求日志不会被在线路径读取或列出。旧主订阅正文也只有被当前 D1 head 指向时才读取。它们因此不会增加请求延迟、KV Read、KV List 或 KV Write，只占用 KV 存储空间。

无需在切换时批量删除旧 KV 键。删除本身会产生 KV 操作，而且误删正文键会破坏当前数据。只有接近 KV 存储容量时，才应在确认 D1 已运行稳定且保留本地备份后离线清理旧前缀。不能删除 D1 中的撤销记录来清缓存。

正常公开请求仍会精确读取必要状态：

- 无关路径、非法请求、未授权管理 API 在存储访问前结束；
- 常规分享请求只从 D1 读取分享状态、正文和撤销状态；极端溢出正文再从 KV 精确读取一次；
- 主订阅从 D1 读取 head，再从 KV 精确读取当前正文；
- 设置使用批量主键读取，公开显示设置缓存 60 秒；
- 分享摘要、节点管理索引和请求统计使用 15 秒实例内缓存；
- 管理视图不轮询，不主动拉取上游。

D1 主库提供一致读取，分享重置及撤销不再受 KV 跨区域最终一致性的延迟影响。KV 正文使用不可变键，发布后不会原地修改。

## 校验与回滚

`npm run pages:build` 执行源码检查、单元测试、构建和产物冒烟；冒烟环境同时提供 MemoryD1 与 MemoryKV。专项测试覆盖 D1 自动建表、批量写事务回滚、旧 KV 隔离、节点有界当前表、100 节点批量事务、日志 TTL、大正文留在 KV 以及 current/previous 清理。

升级顺序是：推送兼容代码、创建并绑定 D1、重新部署、从备份重新录入。缺少 DB 时继续走 KV；DB 生效后以空 D1 为准，不回读旧结构化数据。

若回滚到不支持 D1 的旧代码，旧代码会忽略 D1，只能看到 KV 中仍能识别的数据；D1 生效后新增的设置、索引和撤销状态不会被旧代码读取。回滚可能重新暴露旧链接，因此应优先通过 Pages 重新部署当前代码修复，不应删除数据库或撤销记录。
